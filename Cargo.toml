[package]
name = "ttt_unlimited"
version = "0.1.0"
edition = "2021"

[lib]
name = "ttt_lib"
path = "src/lib.rs"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.16.0", features = ["dynamic_linking"] }
